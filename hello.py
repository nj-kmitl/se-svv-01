# Write a program which reads a string from the user and prints it out in reverse order.

# Get the string from the user
string = input("Enter a string: ")

# Print the string in reverse order
print(string[::-1])